#!/bin/bash
echo "Archived legacy wrapper only. The old render-for-platform.js entrypoint has been removed from the active source tree."
echo "Use the current Ultimate render flow via: node remotion-video/scripts/render-project.mjs"
exit 1
