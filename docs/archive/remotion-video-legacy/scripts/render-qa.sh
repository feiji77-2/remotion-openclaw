#!/bin/bash
echo "Archived legacy wrapper only. The old render-qa.js entrypoint has been removed from the active source tree."
echo "Use the current Ultimate pipeline checks via: npm run release:check"
exit 1
